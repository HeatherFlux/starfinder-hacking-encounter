{
  "name": "starfinder-encounters-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for real-time sync in Damoritosh's Arena",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.11.1",
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4",
    "wrangler": "^3.99.0"
  }
}
